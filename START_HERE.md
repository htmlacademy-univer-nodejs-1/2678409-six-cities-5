# 🚀 НАЧНИТЕ ОТСЮДА - Быстрый гайд (5 минут)

Это краткое руководство для быстрого старта. Если вам нужно больше деталей, смотрите другие файлы.

---

## ⚡ За 5 минут до первого работающего API

### Шаг 1️⃣: Запустить MongoDB (1 минута)

```bash
# Если у вас есть Docker
docker run -d \
  --name mongodb \
  -p 27017:27017 \
  -e MONGO_INITDB_ROOT_USERNAME=root \
  -e MONGO_INITDB_ROOT_PASSWORD=secret \
  mongo:latest

# Или если MongoDB уже установлена локально
# macOS: brew services start mongodb-community
# Linux: sudo systemctl start mongod
```

### Шаг 2️⃣: Запустить приложение (1 минута)

```bash
# Установить зависимости (первый раз)
npm install

# Запустить в development режиме
npm run dev

# Вы должны увидеть:
# Server listening on port: 3000
# Application started
```

### Шаг 3️⃣: Протестировать API (2 минуты)

**Выберите один способ:**

#### 🏃 Способ 1: Быстрый bash скрипт (РЕКОМЕНДУЕТСЯ)

В новом терминале:
```bash
chmod +x QUICK_TEST.sh
./QUICK_TEST.sh
```

Вы увидите зелёные галочки ✓ если всё работает.

#### 🌐 Способ 2: Одна curl команда

```bash
# Получить все предложения
curl http://localhost:3000/offers | jq
```

#### 📱 Способ 3: Postman
1. Скачайте [Postman](https://www.postman.com/downloads/)
2. Создайте запрос: `GET http://localhost:3000/offers`
3. Нажмите Send

#### 💻 Способ 4: VS Code REST Client
1. Установите расширение "REST Client"
2. Создайте файл `api.http`:
   ```http
   ### Получить все предложения
   GET http://localhost:3000/offers
   ```
3. Нажмите "Send Request" над запросом

---

## ✨ Основные endpoints

### Получить все предложения (GET)

```bash
curl http://localhost:3000/offers | jq
```

### Создать предложение (POST)

```bash
curl -X POST http://localhost:3000/offers \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Beautiful apartment",
    "description": "Amazing place to stay",
    "city": "Paris",
    "preview": "https://via.placeholder.com/800x600",
    "images": ["https://via.placeholder.com/800x600"],
    "isPremium": false,
    "type": "apartment",
    "bedrooms": 2,
    "guests": 4,
    "price": 100,
    "amenities": ["WiFi"],
    "coordinates": {
      "latitude": 48.85661,
      "longitude": 2.351499
    }
  }' | jq
```

### Получить одно предложение (GET)

```bash
curl http://localhost:3000/offers/{id} | jq
```

### Обновить предложение (PUT)

```bash
curl -X PUT http://localhost:3000/offers/{id} \
  -H "Content-Type: application/json" \
  -d '{"title": "Updated", "price": 150}' | jq
```

### Удалить предложение (DELETE)

```bash
curl -X DELETE http://localhost:3000/offers/{id}
```

---

## 📚 Что дальше?

- **TESTING_GUIDE.md** - полное руководство по тестированию
- **CONTROLLERS_REFERENCE.md** - все endpoints с примерами
- **ARCHITECTURE_OVERVIEW.md** - как устроено приложение

---

## ✅ Чек-лист первого запуска

- [ ] MongoDB запущена
- [ ] Приложение запущено (npm run dev)
- [ ] Получается запрос: curl http://localhost:3000/offers
- [ ] Ответ пустой массив: []

✓ Если всё работает - поздравляем! 🎉

---

**Время на прочтение:** 5 минут  
**Время на запуск:** 2 минуты  

Удачи! 🚀
